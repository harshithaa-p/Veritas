import './globals.css'
export const metadata = { title: 'Veritas — AI News Verifier', description: 'AI-powered fake news detector' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
